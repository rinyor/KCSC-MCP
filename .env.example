KCSC_API_KEY=replace_with_your_kcsc_api_key
# Optional: set to false only when local TLS inspection breaks KCSC SSL validation.
# KCSC_VERIFY_SSL=true
# Optional: point to a custom CA bundle instead of disabling verification.
# KCSC_CA_BUNDLE=C:\path\to\ca-bundle.pem
